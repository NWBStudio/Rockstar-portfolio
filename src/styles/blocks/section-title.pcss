.section-title {
    font-size: 36px;
    line-height: 48px;
    font-weight: $bold;
    &--dark {
        color: $font-dark-blue;
    }
    &--admin {
        font-size: 21px;
        color: $font-dark-blue;
        line-height: 34px;
    }
    @include phones-small {
        font-size: 30px;
    }
}