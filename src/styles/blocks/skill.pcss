.skill {
    position: relative;
    width: 150px;
    height: 150px;
    &::last-child {
        margin-right: 0;
    }
}

.skill__shape {
    width: 150px;
    height: 150px;
    fill: transparent;
    transform: rotate(180deg);
}

.skill__circle--position--under {
    stroke: $light-grey;
    stroke-width: 10px;
    @include tablets {
        stroke-width: 15px;
    }
    @include tablets {
        stroke-width: 18px;
    }
}

.skill__circle--position--above {
    stroke-width: 20px;
    stroke-dasharray: 502; // Приблизительный расчёт по формуле 2 * π * R  = C <-- Это оно
    stroke-dashoffset: 251;
    @include tablets {
        stroke-width: 25px;
    }
    @include phones {
        stroke-width: 32px;
    }
}

.skill__title {
    font-size: 18px;
    line-height: 30px;
    font-weight: $bold;
    color: $font-dark-purple;
    opacity: 0.5;
    left: 50%;
    position: absolute;
    text-align: center;
    top: 50%;
    transform: translate(-50%,-50%);
}

