.previews {
    display: inline-flex;
}

.previews__item {
    width: 180px;
    height: 100px;
    outline: 3px solid transparent;
    cursor: pointer;
    &--active {
        outline-color: $amber;
        z-index: 10;
    }
}

.previews__pic {
    width: 100%;
    height: 100%;
}